# Representative output index

Small representative outputs tracked in Git:

- Generated `Traj_912118` replica 3: GRO, TPR, index, and
  `analysis/manuscript_static_cne0_result.csv`.
- Reference `Traj_13430`: GRO, TPR, index, and
  `analysis/manuscript_static_cne0_result.csv`.

All are under the corresponding `static_cne0_release/examples/` directories.
The two 50 ns XTC files are not tracked. Expected archive filenames, sizes, and
SHA256 values are recorded in
`static_cne0_release/external_data/representative_trajectory_files.csv`.

